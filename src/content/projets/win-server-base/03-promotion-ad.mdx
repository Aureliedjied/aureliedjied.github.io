---
title: "Promotion Active Directory & DNS"
description: "Installation du rôle AD DS et création d'une nouvelle forêt pour le laboratoire."
tags: ["Windows Server", "Active Directory", "DNS"]
category: "Système Windows"
pubDate: 2026-01-12
slug: "promotion-ad"
---
import SeriesStepper from '../../../components/SeriesStepper.astro';
import { WIN_SERVER_BASIC } from '../../../constants/series';

<SeriesStepper 
  seriesTitle="Déploiement Windows Server 2022" 
  steps={WIN_SERVER_BASIC} 
  currentStepIndex={2} 
/>

## 🌳 Architecture de la Forêt

Pour ce labo, nous partons sur une base saine en créant une nouvelle forêt. 

* **Nom de domaine (FQDN) :** `lab.internal` (ou `votre-nom.local`)
* **Niveau fonctionnel :** Windows Server 2016 (pour garantir une compatibilité maximale si nous ajoutons d'autres DC plus anciens par la suite).



---

## 🛠️ Installation du rôle AD DS

L'installation se déroule en deux phases via le Gestionnaire de serveur :

### 1. Ajout des services de domaine Active Directory
Cette étape installe les binaires nécessaires sur le serveur. J'ai également inclus les **outils d'administration de serveur distant (RSAT)** pour gérer le domaine.

### 2. Promotion du serveur
Une fois les services installés, nous lançons l'assistant de promotion :
* **Option :** "Ajouter une nouvelle forêt".
* **DNS :** Le rôle DNS est installé automatiquement car il est indispensable au fonctionnement de l'AD (enregistrements SRV).
* **Catalogue Global (GC) :** Activé par défaut sur le premier DC.

> **Point de vigilance :** Notez bien le mot de passe du **DSRM** (Directory Services Restore Mode). Il est vital pour restaurer l'AD en cas de corruption de la base de données NTDS.



---

## ✅ Vérifications Post-Promotion

Un bon administrateur vérifie toujours son travail. Voici les tests que j'ai effectués :

1. **Vérification du DNS :** Console `dnsmgmt.msc`, vérification de la présence de la zone `_msdcs.lab.infra`.
2. **Santé de l'AD :** Utilisation de la commande `dcdiag` dans l'invite de commande pour s'assurer que tous les tests de base passent.
3. **Répertoire SYSVOL :** Vérification du partage des dossiers `NETLOGON` et `SYSVOL`.



import NextStep from '../../../components/NextStep.astro';

<NextStep 
  href="/projets/mise-en-place-annuaire" 
  title="Mise en place de l'annuaire" 
/>