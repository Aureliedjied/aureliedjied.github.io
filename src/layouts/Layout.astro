---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!doctype html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <title>Aurélie Djied | Portfolio</title>
    </head>
    <body class="bg-[#050505] text-zinc-300 min-h-screen flex flex-col">
        <div class="fixed inset-0 -z-10 overflow-hidden">
            <div class="absolute top-[-10%] left-[-10%] h-[600px] w-[600px] rounded-full bg-purple-600/10 blur-[120px]"></div>
            <div class="absolute bottom-[0%] right-[-5%] h-[500px] w-[500px] rounded-full bg-blue-600/5 blur-[120px]"></div>
        </div>
        
        <main class="max-w-5xl mx-auto px-6 w-full flex-grow">
            <Header />
            <slot />
        </main>

        <Footer />
    </body>
</html>

<style is:global>
    :root { color-scheme: dark; }
    html { font-family: 'Inter', system-ui, sans-serif; scroll-behavior: smooth; }
</style>